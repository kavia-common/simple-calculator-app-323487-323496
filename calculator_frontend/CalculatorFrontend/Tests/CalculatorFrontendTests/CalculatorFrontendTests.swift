import XCTest
@testable import CalculatorFrontend

final class CalculatorFrontendTests: XCTestCase {
    func testScaffoldBuilds() throws {
        // Basic placeholder to ensure the test target is wired correctly.
        XCTAssertTrue(true)
    }
}
